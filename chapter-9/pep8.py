"""
Для автоматического приведения к pep8 и дополнительной стилизации
 можно использовать утилиту black
"""
import sys, os

from base64 import decodestring, decodebytes, decode, b64decode, b64encode, b16decode, b32decode, b85decode, b16encode

def varystrangeimport(a:int=2, b:float=2):


    print  (a, b)
    return None, a, b

